import React, { useState, useEffect } from 'react'
import Login from './components/login'
import { KeyboardAvoidingView, View, Image, TextInput, StyleSheet, TouchableOpacity, Text, Animated, Keyboard } from 'react-native'


export default function App(){
    return(
        <Login/>
    )
}
